# Diagrams

Native `.drawio` files belong in the domain directory matching their flow. Exported images or PDFs are optional presentation artifacts and must not replace the native source.

Every diagram must identify its matching flow document and Besu `26.7.0` baseline.
